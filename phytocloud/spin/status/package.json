{
  "name": "phyto-status",
  "version": "0.1.0",
  "description": "Public tenant status API — reads SQLite directly",
  "scripts": {
    "build": "npx webpack --mode=production && npx mkdirp target && j2w -i dist.js -d combined-wit -n combined -o target/status.wasm",
    "dev": "npx webpack --mode=development"
  },
  "devDependencies": {
    "@fermyon/spin-sdk": "^2.3.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "mkdirp": "^3.0.1"
  }
}
